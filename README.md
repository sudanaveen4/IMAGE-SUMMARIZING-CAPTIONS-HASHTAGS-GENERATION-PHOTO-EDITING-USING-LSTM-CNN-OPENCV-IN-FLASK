# IMAGE-SUMMARIZING-CAPTIONS-HASHTAGS-GENERATION-PHOTO-EDITING-USING-LSTM-CNN-OPENCV-IN-FLASK
flask application which generates hashtags and captions using lstm and cnn as well as edit images, The picture features will be taken from the CNN model, which was trained on the imagenet dataset, and fed into the LSTM model, which will provide the image captions.
